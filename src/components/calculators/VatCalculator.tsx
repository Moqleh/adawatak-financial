'use client';import{useState}from'react';import{calcVat,toVatDTO}from'@/lib/calculators/vat';import{CalculatorInput,CalculatorShell,ResultCard,ResultSummaryTable}from'.';export function VatCalculator({locale}:{locale:string}){const ar=locale==='ar';const[a,setA]=useState('100');const[r,setR]=useState('15');let x;try{x=toVatDTO(calcVat({mode:'add',netAmount:Number(a),ratePercent:Number(r)}))}catch{}return <CalculatorShell title={ar?'حاسبة ضريبة القيمة المضافة':'VAT Calculator'} results={x?<><ResultCard label={ar?'الإجمالي':'Gross'} value={x.gross}/><ResultSummaryTable rows={[{label:ar?'الضريبة':'VAT',value:x.vat},{label:ar?'الصافي':'Net',value:x.net}]}/></>:<p>{ar?'قيم غير صالحة':'Invalid values'}</p>}><CalculatorInput label={ar?'المبلغ قبل الضريبة':'Net amount'} type="number" min="0" value={a} onChange={e=>setA(e.target.value)}/><CalculatorInput label={ar?'نسبة الضريبة %':'VAT rate %'} type="number" min="0" max="100" value={r} onChange={e=>setR(e.target.value)}/></CalculatorShell>}
