import {getTranslations,setRequestLocale} from 'next-intl/server';import {Link} from '@/i18n/navigation';export default async function Home({params}:{params:Promise<{locale:string}>}){const {locale}=await params;setRequestLocale(locale);const t=await getTranslations('home');return <section className="hero"><div className="container"><div className="eyebrow">{t('eyebrow')}</div><h1>{t('title')}</h1><p className="lead">{t('description')}</p><Link href="/tools" className="cta">{t('cta')}</Link><div className="status">{t('status')}</div></div></section>}
