import CurrencyConverter from'@/components/market/CurrencyConverter';export default async function ConverterPage({params}:{params:Promise<{locale:string}>}){const{locale}=await params;const ar=locale==='ar';return <main className="container"><section className="section"><h1>{ar?'تحويل العملات':'Currency Converter'}</h1><p>{ar?'لن يتم حساب نتيجة قبل توفر سعر صرف موثق مع المصدر ووقت التحديث.':'No conversion result is calculated until a verified rate with source and timestamp is available.'}</p><CurrencyConverter locale={locale}/></section></main>}
