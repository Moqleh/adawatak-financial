export default async function CurrenciesPage({params}:{params:Promise<{locale:string}>}){const{locale}=await params;const ar=locale==='ar';return <main className="container"><section className="section"><h1>{ar?'أسعار العملات':'Currency Rates'}</h1><p>{ar?'الأسعار الحية ستظهر هنا مع المصدر ووقت التحديث وحالة LIVE أو DELAYED أو STALE.':'Live rates will appear here with source, timestamp and LIVE, DELAYED or STALE status.'}</p><p>{ar?'مزود البيانات غير مفعّل بعد — لن نعرض أرقامًا وهمية.':'Data provider is not configured yet — no fabricated rates are shown.'}</p></section></main>}
