export function generateStaticParams(){return[{locale:'ar',mic:'XSAU',ticker:'2222'},{locale:'en',mic:'XNAS',ticker:'AAPL'}]}
export default async function StockPage({params}:{params:Promise<{locale:string;mic:string;ticker:string}>}){const{locale,mic,ticker}=await params;const ar=locale==='ar';return <main className="container"><section className="section"><h1>{ticker.toUpperCase()} · {mic.toUpperCase()}</h1><p>{ar?'سعر السهم مرتبط بالإدراج والسوق المحددين، وليس هناك سعر عالمي مستقل للسهم.':'Stock quotes are specific to the selected listing and exchange; there is no exchange-independent global stock price.'}</p><p>{ar?'السعر والمصدر ووقت التحديث سيظهرون بعد تفعيل مزود السوق.':'Price, source and timestamp will appear after the market provider is configured.'}</p><strong>UNAVAILABLE · provider-not-configured</strong></section></main>}
