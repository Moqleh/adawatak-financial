export default async function ConverterPage({params}:{params:Promise<{locale:string}>}){const{locale}=await params;const ar=locale==='ar';return <main className="container"><section className="section"><h1>{ar?'تحويل العملات':'Currency Converter'}</h1><p>{ar?'المحول جاهز لاستقبال سعر الصرف من طبقة البيانات الموثقة. التفعيل الفعلي بانتظار مزود البيانات.':'The converter is ready for verified FX data. Live conversion awaits provider configuration.'}</p><strong>{ar?'تحت التنفيذ — Provider required':'Under Process — Provider required'}</strong></section></main>}
