import {describe,it,expect} from 'vitest';import {calcMortgage} from '../src/lib/calculators/mortgage';import {M,ZERO,formatCurrency,formatRate,nearlyEqual} from '../src/lib/money';
describe('calcMortgage',()=>{const base={propertyPrice:500000,downPayment:100000,annualRatePercent:5,termMonths:240};it('loan amount',()=>expect(formatCurrency(calcMortgage(base).loanAmount)).toBe('400000.00'));it('LTV',()=>expect(formatRate(calcMortgage(base).ltvPercent)).toBe('80.000000'));it('keeps Decimal',()=>expect(calcMortgage(base).loanAmount.equals(M('400000'))).toBe(true));it('principal invariant',()=>{const r=calcMortgage(base);const sum=r.schedule.reduce((s,row)=>s.plus(row.principal),ZERO);expect(nearlyEqual(sum,r.loanAmount)).toBe(true)});it('rejects excessive down payment',()=>expect(()=>calcMortgage({...base,downPayment:600000})).toThrow(/DOWN_PAYMENT_EXCEEDS_PRICE/));});
