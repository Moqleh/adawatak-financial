import{describe,expect,it}from'vitest';import{calculateVat}from'@/lib/calculators/vat';describe('vat',()=>{it('adds VAT',()=>expect(calculateVat({amount:1000,rate:15,mode:'add'})).toEqual({net:1000,tax:150,gross:1150}));it('extracts VAT',()=>{const r=calculateVat({amount:1150,rate:15,mode:'extract'});expect(r.net).toBeCloseTo(1000);expect(r.tax).toBeCloseTo(150)});it('rejects negative values',()=>expect(()=>calculateVat({amount:-1,rate:15,mode:'add'})).toThrow())});
