'use client';import{useState}from'react';export default function CurrencyConverter({locale}:{locale:string}){const ar=locale==='ar';const[amount,setAmount]=useState('100'),[from,setFrom]=useState('USD'),[to,setTo]=useState('SAR');return <div className="calculator-shell"><div className="calculator-form"><label className="calculator-field"><span>{ar?'المبلغ':'Amount'}</span><input type="number" min="0" value={amount} onChange={e=>setAmount(e.target.value)}/></label><label className="calculator-field"><span>{ar?'من':'From'}</span><input value={from} maxLength={3} onChange={e=>setFrom(e.target.value.toUpperCase())}/></label><label className="calculator-field"><span>{ar?'إلى':'To'}</span><input value={to} maxLength={3} onChange={e=>setTo(e.target.value.toUpperCase())}/></label><button type="button" onClick={()=>{const a=from;setFrom(to);setTo(a)}}>{ar?'تبديل العملات':'Swap currencies'}</button></div><aside className="calculator-results" aria-live="polite"><strong>{ar?'بانتظار مزود سعر الصرف الموثق':'Waiting for verified FX provider'}</strong><p>{amount} {from} → {to}</p><small>UNAVAILABLE · provider-not-configured</small></aside></div>}
