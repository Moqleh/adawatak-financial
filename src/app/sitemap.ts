export const dynamic='force-static';
import type{MetadataRoute}from'next';import{tools}from'@/lib/tools/registry';const base=process.env.NEXT_PUBLIC_SITE_URL??'https://moqleh.github.io/adawatak-financial';export default function sitemap():MetadataRoute.Sitemap{const fixed=['','/tools','/favorites','/gold','/currencies','/currency-converter','/markets','/about','/privacy','/disclaimer','/guides'];return['ar','en'].flatMap(locale=>[...fixed.map(path=>({url:`${base}/${locale}${path}/`,changeFrequency:'weekly' as const,priority:path===''?1:.7})),...tools.map(t=>({url:`${base}/${locale}/tools/${t.id}/`,changeFrequency:'monthly' as const,priority:.6}))])}
