import{FavoritesList}from'@/components/tools/FavoritesList';export default async function FavoritesPage({params}:{params:Promise<{locale:string}>}){const{locale}=await params;const ar=locale==='ar';return <><section className="page-hero"><div className="container"><span className="eyebrow">{ar?'محفوظ على جهازك':'Saved on this device'}</span><h1>{ar?'المفضلة':'Favorites'}</h1><p>{ar?'تُحفظ مفضلتك محليًا في هذا المتصفح ولا تتطلب حسابًا.':'Favorites are stored locally in this browser and require no account.'}</p></div></section><section className="tools-section"><div className="container"><FavoritesList locale={locale}/></div></section></>}
