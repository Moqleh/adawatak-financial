import CurrencyConverter from'@/components/market/CurrencyConverter';export default async function ConverterPage({params}:{params:Promise<{locale:string}>}){const{locale}=await params;const ar=locale==='ar';return <main className="container"><section className="section"><span className="eyebrow">{ar?'تحويل بسعر مرجعي':'Reference-rate conversion'}</span><h1>{ar?'تحويل العملات':'Currency Converter'}</h1><p>{ar?'النتيجة تعتمد على أحدث سعر مرجعي متاح وتعرض المصدر وتاريخ السعر بوضوح.':'Conversion uses the latest available reference rate and clearly shows its source and date.'}</p><CurrencyConverter locale={locale}/></section></main>}
