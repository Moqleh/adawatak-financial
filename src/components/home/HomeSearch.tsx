'use client';import{useMemo,useState}from'react';import{Search}from'lucide-react';import{Link,useRouter}from'@/i18n/navigation';type Item={title:string;desc:string;href:string};export default function HomeSearch({items,placeholder}:{items:Item[];placeholder:string}){const[q,setQ]=useState('');const router=useRouter();const results=useMemo(()=>{const term=q.trim().toLocaleLowerCase();if(!term)return[];return items.filter(x=>(x.title+' '+x.desc).toLocaleLowerCase().includes(term)).slice(0,6)},[q,items]);const go=()=>{if(results[0])router.push(results[0].href)};return <div className="live-search"><div className="hero-search"><input value={q} onChange={e=>setQ(e.target.value)} onKeyDown={e=>{if(e.key==='Enter'){e.preventDefault();go()}}} placeholder={placeholder} aria-label={placeholder}/><button type="button" onClick={go} aria-label={placeholder}><Search size={22}/></button></div>{results.length>0&&<div className="search-results">{results.map(x=><Link key={x.href} href={x.href} onClick={()=>setQ('')}><strong>{x.title}</strong><small>{x.desc}</small></Link>)}</div>}</div>}