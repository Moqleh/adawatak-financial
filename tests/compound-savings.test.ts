import{describe,expect,it}from'vitest';import{calculateCompoundSavings}from'@/lib/calculators/compound-savings';describe('compound savings',()=>{it('works at zero return',()=>{const r=calculateCompoundSavings({initial:1000,monthlyContribution:100,annualRate:0,years:1});expect(r.finalValue).toBe(2200);expect(r.growth).toBe(0)});it('grows with positive return',()=>expect(calculateCompoundSavings({initial:1000,monthlyContribution:100,annualRate:6,years:2}).growth).toBeGreaterThan(0))});
